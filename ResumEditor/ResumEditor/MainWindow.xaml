<Window x:Class="ResumEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Resume Editor" Height="700" Width="900">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Static Info -->
            <RowDefinition Height="2*"/>
            <!-- Dynamic Resume Input -->
            <RowDefinition Height="Auto"/>
            <!-- Parse Button -->
            <RowDefinition Height="3*"/>
            <!-- Result Preview -->
        </Grid.RowDefinitions>

        <!-- Static User Info -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <StackPanel Margin="0,0,20,0">
                <TextBlock Text="Name"/>
                <TextBox x:Name="NameBox" Width="200"/>
            </StackPanel>
            <StackPanel Margin="0,0,20,0">
                <TextBlock Text="Title"/>
                <TextBox x:Name="TitleBox" Width="200"/>
            </StackPanel>
            <StackPanel>
                <TextBlock Text="Location"/>
                <TextBox x:Name="LocationBox" Width="200"/>
            </StackPanel>
        </StackPanel>

        <!-- Dynamic Resume Input -->
        <TextBox x:Name="ResumeInput"
                 Grid.Row="1"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"
                 FontFamily="Consolas"
                 FontSize="14"
                 Margin="0,0,0,10"/>

        <!-- Parse Button -->
        <Button Grid.Row="2"
                Content="Parse Resume"
                Width="120"
                HorizontalAlignment="Left"
                Margin="0,0,0,10"
                Click="ParseResume_Click"/>

        <!-- Result Preview -->
        <TextBox x:Name="ParseResult"
                 Grid.Row="3"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"
                 FontFamily="Consolas"
                 FontSize="14"/>
    </Grid>
</Window>
